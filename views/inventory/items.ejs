<!-- Inserting the main content headings  -->
<!-- And making the title value as our h1 value with an if statement. -->
<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<%# messages() %>

<!-- Inserting the main content of the view, which is the  -->
<!-- flex item string the utilities function  -->
<!-- have build out in a string. -->

<%- flex %>